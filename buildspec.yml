version: 0.2

#env:
  #variables:
     # key: "value"
     # key: "value"
  #parameter-store:
     # key: "value"
     # key: "value"

phases:
  #install:
    #commands:
      # - command
      # - command
  #pre_build:
    #commands:
      # - command
      # - command
  build:
    commands:
       - mvn package
      # - command
  #post_build:
    #commands:
      # - command
      # - command
artifacts:
  files:
     - target/webserver-0.0.1-SNAPSHOT.jar
     - appspec.yml
     - scripts/*
  discard-paths: yes
#base-directory: location